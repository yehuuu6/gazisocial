@props(['title', 'description'])
<div class="p-4">
    <h3 class="font-medium text-base 2xl:text-lg text-gray-800">{{ $title }}</h3>
    <span class="text-gray-500 text-xs 2xl:text-sm font-normal">{{ $description }}</span>
</div>
